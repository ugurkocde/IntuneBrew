#!/bin/bash
# Uninstall script for WeekToDo
# Generated by IntuneBrew

# Exit on error
set -e

echo "Uninstalling WeekToDo..."

# Check if running as root
if [ "$EUID" -ne 0 ]; then
  echo "Please run as root"
  exit 1
fi

# Kill application process if running
echo "Stopping WeekToDo if running..."
pkill -f "WeekToDo" 2>/dev/null || true

# Remove /Applications/WeekToDo.app
echo "Removing /Applications/WeekToDo.app..."
if [ -d "/Applications/WeekToDo.app" ]; then
    rm -rf "/Applications/WeekToDo.app" 2>/dev/null || true
elif [ -f "/Applications/WeekToDo.app" ]; then
    rm -f "/Applications/WeekToDo.app" 2>/dev/null || true
fi

# Remove $HOME/Library/Application Support/WeekToDo
echo "Removing $HOME/Library/Application Support/WeekToDo..."
if [ -d "$HOME/Library/Application Support/WeekToDo" ]; then
    rm -rf "$HOME/Library/Application Support/WeekToDo" 2>/dev/null || true
elif [ -f "$HOME/Library/Application Support/WeekToDo" ]; then
    rm -f "$HOME/Library/Application Support/WeekToDo" 2>/dev/null || true
fi

# Remove $HOME/Library/Preferences/weektodo-app.netlify.app.plist
echo "Removing $HOME/Library/Preferences/weektodo-app.netlify.app.plist..."
if [ -d "$HOME/Library/Preferences/weektodo-app.netlify.app.plist" ]; then
    rm -rf "$HOME/Library/Preferences/weektodo-app.netlify.app.plist" 2>/dev/null || true
elif [ -f "$HOME/Library/Preferences/weektodo-app.netlify.app.plist" ]; then
    rm -f "$HOME/Library/Preferences/weektodo-app.netlify.app.plist" 2>/dev/null || true
fi

# Remove $HOME/Library/Saved Application State/weektodo-app.netlify.app.savedState
echo "Removing $HOME/Library/Saved Application State/weektodo-app.netlify.app.savedState..."
if [ -d "$HOME/Library/Saved Application State/weektodo-app.netlify.app.savedState" ]; then
    rm -rf "$HOME/Library/Saved Application State/weektodo-app.netlify.app.savedState" 2>/dev/null || true
elif [ -f "$HOME/Library/Saved Application State/weektodo-app.netlify.app.savedState" ]; then
    rm -f "$HOME/Library/Saved Application State/weektodo-app.netlify.app.savedState" 2>/dev/null || true
fi

echo "Uninstallation complete!"
exit 0
