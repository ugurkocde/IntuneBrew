#!/bin/bash
# Uninstall script for Modern CSV
# Generated by IntuneBrew

# Exit on error
set -e

echo "Uninstalling Modern CSV..."

# Check if running as root
if [ "$EUID" -ne 0 ]; then
  echo "Please run as root"
  exit 1
fi

# Kill application process if running
echo "Stopping Modern CSV if running..."
pkill -f "Modern CSV" 2>/dev/null || true

# Remove /Applications/Modern CSV.app
echo "Removing /Applications/Modern CSV.app..."
if [ -d "/Applications/Modern CSV.app" ]; then
    rm -rf "/Applications/Modern CSV.app" 2>/dev/null || true
elif [ -f "/Applications/Modern CSV.app" ]; then
    rm -f "/Applications/Modern CSV.app" 2>/dev/null || true
fi

# Remove $HOME/Library/Application Support/Modern CSV
echo "Removing $HOME/Library/Application Support/Modern CSV..."
if [ -d "$HOME/Library/Application Support/Modern CSV" ]; then
    rm -rf "$HOME/Library/Application Support/Modern CSV" 2>/dev/null || true
elif [ -f "$HOME/Library/Application Support/Modern CSV" ]; then
    rm -f "$HOME/Library/Application Support/Modern CSV" 2>/dev/null || true
fi

# Remove $HOME/Library/Saved Application State/net.galliumdigital.Modern-CSV.savedState
echo "Removing $HOME/Library/Saved Application State/net.galliumdigital.Modern-CSV.savedState..."
if [ -d "$HOME/Library/Saved Application State/net.galliumdigital.Modern-CSV.savedState" ]; then
    rm -rf "$HOME/Library/Saved Application State/net.galliumdigital.Modern-CSV.savedState" 2>/dev/null || true
elif [ -f "$HOME/Library/Saved Application State/net.galliumdigital.Modern-CSV.savedState" ]; then
    rm -f "$HOME/Library/Saved Application State/net.galliumdigital.Modern-CSV.savedState" 2>/dev/null || true
fi

echo "Uninstallation complete!"
exit 0
